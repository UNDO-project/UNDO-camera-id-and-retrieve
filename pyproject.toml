[project]
name = "cctv-identification-and-research"
version = "0.4.0"
description = "A pipeline to scrape cctv data and identify given cameras in urban settings."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "httpx>=0.28.1",
    "ipykernel>=7.1.0",
    "loguru>=0.7.3",
    "open-clip-torch>=3.2.0",
    "pandas>=2.3.3",
    "pillow>=12.0.0",
    "playwright>=1.57.0",
    "pre-commit>=4.3.0",
    "pyarrow>=22.0.0",
    "pydantic>=2.12.3",
    "pytest>=9.0.0",
    "python-dotenv>=1.2.1",
    "ruff>=0.14.10",
    "sphinx>=8.2.3",
    "sphinx-rtd-theme>=3.0.2",
    "torch>=2.9.1",
    "ultralytics>=8.3.228",
]

[project.scripts]
cidar-scrape = "src.scraping.main:main"
cidar-build = "src.building.main:main"
cidar-validate = "src.validation.cli:main"
cidar-identify = "src.identification.cli:main"
